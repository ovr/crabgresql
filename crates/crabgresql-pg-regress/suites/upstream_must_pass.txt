# Upstream regression tests (vendor/postgres/regress) that must pass.
# `cargo test -p crabgresql-pg-regress` fails if any test listed here regresses.
# Promote tests here as SQL coverage grows; one name per line.
boolean
circle
comments
float4
float8
line
lseg
macaddr
macaddr8
md5
money
path
portals_p2
time
varchar
